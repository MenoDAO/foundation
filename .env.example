# Server-only. Used by /api/forms to proxy submissions.
# Never put credentials, tokens, or database URLs here.
API_URL=

# Browser-visible. Used to load published clinic and impact data.
# This value is compiled into the client bundle — never put secrets here.
NEXT_PUBLIC_API_URL=
